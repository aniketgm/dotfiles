windows:
  dotfiles: ~/.dotfiles
  link_type: 'symbolic'
  shell_command: powershell -noprofile -nologo -c {{ quote "" cmd }}

linux:
  dotfiles: ~/.dotfiles
  link_type: 'hard'
  shell_command: bash -c {{ quote "" cmd }}
